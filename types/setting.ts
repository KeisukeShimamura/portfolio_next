import { Image } from "./work";

export type setting = {
  aboutTitle: string;
  aboutNote: string;
  aboutImage: Image;
  github: string;
  createdAt: string;
  updatedAt: string;
  publishedAt: string;
  revisedAt: string;
}